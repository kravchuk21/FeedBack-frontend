import React from 'react'
import {Typography,} from '../UI'

const Empty = () => {
	return (
		<div className="flex justify-center m-10">
			<Typography fontWeight={700} size={86}>😔</Typography>
		</div>
	)
}

export default Empty
