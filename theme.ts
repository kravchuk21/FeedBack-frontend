import {ThemeType,} from './src/components/UI'

export const Theme: ThemeType = {
	typography: 'var(--typography)',
	typographyLight: 'var(--typography-light)',
	primary: 'var(--primary)',
	primaryLight: 'var(--primary-light)',
	danger: 'var(--error)',
	dangerLight: 'var(--error-light)',
	base: 'var(--base)',
	baseLight: 'var(--base-light)',
	warning: 'var(--warning)',
	bg: 'var(--bg)',
	borderRadius: 16,
}
